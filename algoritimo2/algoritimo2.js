alert('Calcular a área e o perimetro de um quadrado')

const lado = Number(prompt('Digite o tamanho o lado do quadrado'))
const area = Math.pow(lado, 2)
alert('Área do quadrado: ' + area)

calculo = lado * 4
alert('Perímetro o quadrado é: ' + area)